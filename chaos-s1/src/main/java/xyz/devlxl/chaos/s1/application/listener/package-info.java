/**
 * 领域事件监听者，包括内部监听（通过Spring Event Listener）和外部监听（通过Spring Cloud Stream）。
 * 
 * Domain event listeners, including internal listeners (via Spring Event Listener) and external listeners (via Spring
 * Cloud Stream)
 * 
 * @author Liu Xiaolei
 * @date 2018/08/27
 */
package xyz.devlxl.chaos.s1.application.listener;